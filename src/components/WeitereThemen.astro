---
import { getCollection } from "astro:content";

const posts = await getCollection("verstehen");

const current = Astro.url.pathname;

const related = posts
  .filter((p) => !current.includes(p.slug))
  .slice(0, 6);
---

<section class="mt-12">
  <h2 class="text-xl font-semibold mb-4">
    Weitere hilfreiche Themen
  </h2>

  <p class="text-sm text-neutral-600 mb-4 max-w-2xl">
    Wenn Sie die Unterschiede noch besser verstehen möchten, helfen diese kurzen Erklärseiten beim Einordnen.
  </p>

  <ul class="space-y-2">
    {related.map((post) => (
      <li>
        <a
          href={`/verstehen/${post.slug}/`}
          class="text-blue-600 hover:underline"
        >
          {post.data.title}
        </a>
      </li>
    ))}
  </ul>
</section>